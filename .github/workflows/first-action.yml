# This is the first workflow, to make your first

name: First Workflow

on: workflow_dispatch
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
              - name: Print Greeting
                run: echo "Hello, World !"
              - name: Print GoodBye
                run: echo "Good Bye !"          

